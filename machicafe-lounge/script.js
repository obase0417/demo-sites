(() => {const header=document.querySelector('.site-header');const hero=document.querySelector('.hero');const mobileCta=document.querySelector('.mobile-cta');const parallaxItems=[...document.querySelectorAll('.parallax')];const revealItems=[...document.querySelectorAll('.reveal')];const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let lastScrollY=window.scrollY;let ticking=false;const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');entry.target.closest('.about')?.classList.add('is-visible');observer.unobserve(entry.target)})},{threshold:.16});revealItems.forEach((item,index)=>{item.style.transitionDelay=`${index%3*90}ms`;observer.observe(item)});const update=()=>{const y=window.scrollY;header.classList.toggle('is-scrolled',y>20);header.classList.toggle('is-hidden',y>lastScrollY&&y>180);mobileCta.classList.toggle('is-shown',y>hero.offsetHeight*.7);if(!reducedMotion)parallaxItems.forEach((item,index)=>{const rect=item.getBoundingClientRect();const progress=(innerHeight-rect.top)/(innerHeight+rect.height);const shift=(progress-.5)*(index%2?20:-16);item.classList.add('will-animate');item.style.transform=`translate3d(0, ${shift}px, 0) scale(1.035)`});lastScrollY=y;ticking=false};const onScroll=()=>{if(!ticking){requestAnimationFrame(update);ticking=true}};addEventListener('scroll',onScroll,{passive:true});addEventListener('resize',onScroll,{passive:true});addEventListener('load',()=>{document.querySelector('.hero__image')?.classList.remove('will-animate');onScroll()},{once:true});onScroll()})();
